<h1>Chatroom#index</h1>
<p>Find me in app/views/chatroom/index.html.erb</p>
<div class="chatroom">
  <div class="chatroom-thread">
    <%= render @messages %>
  </div>
  <div class="chatroom-new">
    <% if logged_in? %>
      <%= render "messages/message_form" %>
    <% end %>
  </div>
</div>
